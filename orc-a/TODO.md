# TODO — before making the repo public

Tracking items that still need attention before ORC is deployed as a public
open-source project. Grouped by who/what blocks them.

## Repo bootstrap

- [ ] **Create a fresh public GitHub repo**, move the code there, and push a
      single clean initial commit (no prior history). Update remote URLs in
      README, CITATION.cff, and any other references afterward.

## Repo settings (GitHub admin)

- [ ] **Enable GitHub Pages.** Settings → Pages → Source: *GitHub Actions*.
      Needed for `.github/workflows/docs.yml` to publish Sphinx docs.
- [ ] **Create the `pypi` environment.** Settings → Environments → *New
      environment* named `pypi`. Optionally add required reviewers.
- [ ] **Configure PyPI Trusted Publisher.**
      <https://pypi.org/manage/account/publishing/> — workflow `release.yml`,
      environment `pypi`, project `orcpy`. Do this *before* the first tag push.
- [ ] **Install the Codecov app** on the repo so the CI coverage upload
      (OIDC, tokenless) has somewhere to go.
- [ ] **Enable GitHub Discussions** (Settings → General → Features) — referenced
      from the issue template's "Questions and discussions" link.
- [ ] **Set branch protection on `main`:** require PR, require CI green,
      require at least one review, disallow force-push.
- [ ] **Enable private vulnerability reporting** (Settings → Security →
      Advanced) so `SECURITY.md`'s "Report a vulnerability" flow works.

## Placeholder values to replace

- [ ] `TODO@example.org` in [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md) and
      [`SECURITY.md`](SECURITY.md) → real contact email.

## Authorship sign-off

- [ ] Each co-author must confirm they consent to being listed in
      [`LICENSE`](LICENSE) and
      [`CITATION.cff`](CITATION.cff) with their name and
      `surname@anonymous.com` email. Authors who prefer to opt out or
      use a different email address should raise an issue / PR before the
      first tagged release.

## First release checklist

- [ ] Pick first version (`0.1.0` suggested). Set in:
      - [`CITATION.cff`](CITATION.cff) — uncomment `version`, `date-released`.
      - [`pyproject.toml`](pyproject.toml).
- [ ] Tag the release (`git tag v0.1.0 && git push --tags`) — triggers
      `release.yml`.
- [ ] After Zenodo archives the release, copy the DOI into
      [`CITATION.cff`](CITATION.cff) `doi:`.
- [ ] Link the Zenodo record in the release notes.

## Code-quality follow-ups

- [ ] **First formatter pass.** The codebase currently has mixed tab/space
      indentation.
      ```bash
      pre-commit install
      pre-commit run --all-files
      ```
      Commit the resulting reformat in a dedicated commit (separate from any
      behavior change) so the diff is easy to review and revert.
- [ ] Re-enable the currently `continue-on-error: true` CI jobs as the tree
      cleans up:
      - `sanitizers` (ASan+UBSan) — fix any findings, then drop the flag.
      - `clang-tidy` — triage warnings, tighten `.clang-tidy`, then drop the flag.
      - `coverage` — keep tolerant until a floor is established, then set a
        Codecov target.
- [ ] **Fix broken C++ examples.** `examples/cpp/jointspace_trajectory_example.cpp`
      and `serialization_example.cpp` fail to compile (`traj.execute(t)` no
      longer exists). Either update them or remove them from
      `examples/cpp/CMakeLists.txt`.
- [ ] Consider a `.gitattributes` with `* text=auto eol=lf` to prevent CRLF
      leakage from Windows contributors.
- [ ] Run `reuse lint` once and decide whether to add SPDX headers to every
      source file (optional; LICENSE alone is sufficient for BSD-3-Clause).

## Nice-to-have

- [ ] ORCID on `CITATION.cff` author entry.
- [ ] `CHANGELOG.md` autogenerated from conventional-commit history
      (`git-cliff` or `release-please`).
- [ ] README "Cookbook" section with 3–4 small end-to-end recipes linked
      from the docs.
