#if defined(TC_BUILD) && !defined(SSTREAM_H)
#define SSTREAM_H

	namespace std {
		class ostream {
		public:

			ostream() {

			}
			void operator<<(const float& b) {
				return;
			}
		};
	//if defined(_DEFINE_STD_RAND_)
		inline int rand() {
			static unsigned int seed = 12345;
			seed = seed * 1103515245 + 12345;
			return (seed / 65536) % 32768;
		}
		//#endif
	}

#endif; 


